# Sophie & Alex — Save the Date

An interactive wedding invitation that opens from a paper envelope. The SVG is
mounted only after the invitation finishes sliding out, so its internal drawing
animations always begin at the intended moment.

## Run locally

```bash
npm install
npm run dev
```

## Verify

```bash
npm test
```
